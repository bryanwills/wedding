---
export interface Props {
  className?: string
}

const { className = '' } = Astro.props
---

<article class={`px-8 mx-auto max-w-3xl ${className}`}><slot /></article>
<!-- <article class={`px-8 mx-auto grid grid-cols-1 md:grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4`}><slot /></article> -->
